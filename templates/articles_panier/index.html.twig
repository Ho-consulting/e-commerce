{% extends 'layouts/base.html.twig' %}

{% block title %}ArticlesPanier index{% endblock %}

{% block body %}
    <h1>ArticlesPanier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for articles_panier in articles_paniers %}
            <tr>
                <td>{{ articles_panier.id }}</td>
                <td>{{ articles_panier.quantity }}</td>
                <td>
                    <a href="{{ path('app_articles_panier_show', {'id': articles_panier.id}) }}">show</a>
                    <a href="{{ path('app_articles_panier_edit', {'id': articles_panier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_articles_panier_new') }}">Create new</a>
{% endblock %}
