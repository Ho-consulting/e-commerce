{% extends 'layouts/base.html.twig' %}

{% block title %}ArticlesPanier{% endblock %}

{% block body %}
    <h1>ArticlesPanier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ articles_panier.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ articles_panier.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_articles_panier_index') }}">back to list</a>

    <a href="{{ path('app_articles_panier_edit', {'id': articles_panier.id}) }}">edit</a>

    {{ include('articles_panier/_delete_form.html.twig') }}
{% endblock %}
